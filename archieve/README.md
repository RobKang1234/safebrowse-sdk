# Archive

This directory preserves pre-V6 SafeBrowse source, docs, configs, and harness files for historical reference only.

Everything under `archieve/` is outside the active V6 release contract, outside the active audit claim, and outside the supported public API surface.
